`root@bugs-VMware-Virtual-Platform:/home/bugs/ansible_playbooks/roles/spill_injector_role/tasks# cat main.yml`
```bash
---
- name: Ensure Documents directory and copy files
  hosts: linux  # Change this to your actual target host group
  become: yes   # Use become if you need elevated privileges

  tasks:
    - name: Ensure /home/bugs/Documents directory exists
      file:
        path: /home/bugs/Documents
        state: directory
        mode: '0755'

    - name: Copy Stargate credentials PDF to /home/bugs/Documents
      copy:
        src: /home/bugs/ansible_playbooks/files/stargate_credentials.pdf
        dest: /home/bugs/Documents/stargate_credentials.pdf
        owner: bugs
        group: bugs
        mode: '0644'

    - name: Verify file exists in /home/bugs/Documents
      stat:
        path: /home/bugs/Documents/stargate_credentials.pdf
      register: pdf_file

    - name: Output message if PDF exists
      debug:
        msg: "Stargate credentials PDF has been successfully copied."
      when: pdf_file.stat.exists

    - name: List contents of /home/bugs/Documents
      command: ls -l /home/bugs/Documents
      register: documents_list

    - name: Output directory listing
      debug:
        var: documents_list.stdout_lines
```



![[Pasted image 20241021103859.png]]
