Original
```bash
┌──(root💀gobots)-[13Jul2024 00:03:28]-[/home/…/SUT/HITCON_CTF/RClonE/dist]
└─# cat docker-compose.yml 
Device "veth3b7c8e5@if75" does not exist.
Device "veth3b7c8e5@if75" does not exist.
services:
  rclone:
    image: rclone
    build: .
    environment:
      - SECRET=secret  # randomized secret per instancer
    networks:
      - chall
  bot:
    image: rclone-bot
    build: ./bot
    environment:
      - TITLE=Admin Bot for RClonE
      - PORT=8000
      - URL_CHECK_REGEX=^https?://.{1,256}$
      - SECRET=secret  # randomized secret per instancer
    security_opt: 
      - seccomp=chrome.json
    ports:
      - "${PORT}:8000"
    networks:
      - default
      - chall
networks:
  chall:
    internal: true
```
Modified
```bash
┌──(root💀gobots)-[13Jul2024 00:03:28]-[/home/…/SUT/HITCON_CTF/RClonE/dist]
└─# cat docker-compose.yml 
Device "veth3b7c8e5@if75" does not exist.
Device "veth3b7c8e5@if75" does not exist.
services:
  rclone:
    image: rclone
    build: .
    environment:
      - SECRET=secret  # randomized secret per instancer
    networks:
      - chall
  bot:
    image: rclone-bot
    build: ./bot
    environment:
      - TITLE=Admin Bot for RClonE
      - PORT=8000
      - SECRET=secret  # randomized secret per instancer
    security_opt: 
      - seccomp=chrome.json
    ports:
      - "${PORT}:8000"
    networks:
      - default
      - chall
networks:
  chall:
    internal: true
```